import { ToolButton } from "widgets.slint";

export component TopBar inherits Rectangle {
    in property <bool> dark;
    in property <string> current-path;
    in property <float> sidebar-width-px;
    in property <length> divider-offset: 3px;
    in property <bool> can-go-back;
    in property <bool> can-go-forward;
    in property <bool> search-active;
    in-out property <string> path-text;
    in-out property <bool> path-focused;
    in-out property <string> search-query;
    in-out property <bool> search-focused;
    callback go_back();
    callback go_forward();
    callback go_parent();
    callback path_submitted(string);
    callback search_requested();
    callback search_submitted(string);
    callback search_close_requested();
    callback dark_toggled();

    height: 64px;
    background: root.dark ? #0f1214 : #fbfcfd;
    border-width: 0px;

    HorizontalLayout {
        padding-right: 18px;
        padding-top: 12px;
        padding-bottom: 12px;
        spacing: 0px;

        HorizontalLayout {
            width: root.sidebar-width-px * 1px + root.divider-offset;
            padding-left: 18px;
            padding-right: 18px;
            spacing: 6px;

            ToolButton {
                label: "<";
                dark: root.dark;
                enabled: root.can-go-back;
                clicked => { root.go_back(); }
            }

            ToolButton {
                label: ">";
                dark: root.dark;
                enabled: root.can-go-forward;
                clicked => { root.go_forward(); }
            }

            ToolButton {
                label: "^";
                dark: root.dark;
                clicked => { root.go_parent(); }
            }
        }

        Rectangle {
            horizontal-stretch: 1;
            min-width: 120px;
            height: 40px;
            border-radius: 8px;
            border-width: 1px;
            border-color: root.path-focused ? (root.dark ? #58b6dd : #2b86b7) : (root.dark ? #30363c : #d8e0e8);
            background: root.path-focused ? (root.dark ? #15191c : #ffffff) : (root.dark ? #15191c : #f5f7fa);

            Text {
                visible: root.path-text == "";
                x: 12px;
                width: parent.width - 24px;
                height: parent.height;
                text: "/path/to/folder";
                color: root.dark ? #74808a : #697684;
                font-size: 14px;
                overflow: elide;
                vertical-alignment: center;
            }

            TextInput {
                x: 12px;
                width: parent.width - 24px;
                height: parent.height;
                text <=> root.path-text;
                single-line: true;
                font-size: 14px;
                vertical-alignment: center;
                color: root.dark ? #f4f7fa : #1f2933;
                selection-background-color: root.dark ? #51a9d4 : #2b86b7;
                selection-foreground-color: root.dark ? #0f172a : #ffffff;
                accepted => { root.path_submitted(self.text); }
                changed has-focus => {
                    root.path-focused = self.has-focus;
                    if (!self.has-focus) {
                        self.text = root.current-path;
                    } else if (self.text == "") {
                        self.text = root.current-path;
                    }
                }
            }
        }

        HorizontalLayout {
            padding-left: 10px;
            spacing: 8px;

            if (!root.search-active): Text {
                text: "Split";
                color: root.dark ? #dce3ea : #526071;
                font-size: 14px;
                font-weight: 500;
                height: 40px;
                min-width: 34px;
                vertical-alignment: center;
            }

            if (!root.search-active): ToolButton {
                label: "S";
                dark: root.dark;
                clicked => { root.search_requested(); }
            }

            if (root.search-active): Rectangle {
                width: 240px;
                height: 40px;
                border-radius: 8px;
                border-width: 1px;
                border-color: root.search-focused ? (root.dark ? #58b6dd : #2b86b7) : (root.dark ? #30363c : #d8e0e8);
                background: root.search-focused ? (root.dark ? #15191c : #ffffff) : (root.dark ? #15191c : #f5f7fa);

                Text {
                    x: 11px;
                    width: 18px;
                    height: parent.height;
                    text: "S";
                    color: root.dark ? #8b98a5 : #667586;
                    font-size: 13px;
                    font-weight: 800;
                    horizontal-alignment: center;
                    vertical-alignment: center;
                }

                Text {
                    visible: root.search-query == "";
                    x: 36px;
                    width: parent.width - 72px;
                    height: parent.height;
                    text: "Search";
                    color: root.dark ? #74808a : #697684;
                    font-size: 14px;
                    overflow: elide;
                    vertical-alignment: center;
                }

                TextInput {
                    x: 36px;
                    width: parent.width - 72px;
                    height: parent.height;
                    text <=> root.search-query;
                    single-line: true;
                    font-size: 14px;
                    vertical-alignment: center;
                    color: root.dark ? #f4f7fa : #1f2933;
                    selection-background-color: root.dark ? #51a9d4 : #2b86b7;
                    selection-foreground-color: root.dark ? #0f172a : #ffffff;
                    accepted => { root.search_submitted(self.text); }
                    changed has-focus => {
                        root.search-focused = self.has-focus;
                    }
                }

                close-search-touch := TouchArea {
                    x: parent.width - 34px;
                    width: 34px;
                    height: parent.height;
                    clicked => { root.search_close_requested(); }
                }

                Rectangle {
                    x: parent.width - 28px;
                    y: (parent.height - self.height) / 2;
                    width: 22px;
                    height: 22px;
                    border-radius: 6px;
                    background: close-search-touch.has-hover ? (root.dark ? #2b3035 : #e9eef4) : transparent;

                    Text {
                        text: "X";
                        color: root.dark ? #dce3ea : #526071;
                        font-size: 12px;
                        font-weight: 800;
                        horizontal-alignment: center;
                        vertical-alignment: center;
                    }
                }
            }

            ToolButton {
                label: root.dark ? "L" : "D";
                dark: root.dark;
                clicked => { root.dark_toggled(); }
            }
        }
    }

    Rectangle {
        y: parent.height - 1px;
        width: parent.width;
        height: 1px;
        background: root.dark ? #252b31 : #e7edf3;
    }
}
