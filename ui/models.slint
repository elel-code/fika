export struct FileEntry {
    name: string,
    path: string,
    group: string,
    location: string,
    kind: string,
    size: string,
    size_bytes: float,
    modified: string,
    modified_age_days: int,
    is_dir: bool,
    selected: bool,
    thumbnail_state: int,
    thumbnail: image,
}

export struct PlaceEntry {
    label: string,
    path: string,
    marker: string,
    is_builtin: bool,
}

export struct DeviceEntry {
    label: string,
    path: string,
    device_path: string,
    kind: string,
    marker: string,
    mounted: bool,
    can_mount: bool,
    can_unmount: bool,
    can_eject: bool,
    pending_action: string,
    error: string,
}

export struct DesktopApp {
    id: string,
    name: string,
    is_default: bool,
}

export struct ContextServiceAction {
    id: string,
    name: string,
}

export struct ChooserChoiceOption {
    label: string,
}

export struct ChooserChoice {
    label: string,
    selected_label: string,
    selected_index: int,
    options: [ChooserChoiceOption],
}

export struct PaneSlotData {
    slot: int,
    current_path: string,
    path_text: string,
    path_focused: bool,
    can_go_back: bool,
    can_go_forward: bool,
    search_panel_visible: bool,
    search_panel_height_px: float,
    search_loading: bool,
    search_query: string,
    recursive_search: bool,
    search_kind_filter: int,
    search_modified_filter: int,
    search_size_filter: int,
    search_filters_active: bool,
    search_kind_label: string,
    search_modified_label: string,
    search_size_label: string,
    content_interactive: bool,
    drop_ready: bool,
    drop_trace_prefix: string,
    entry_count: int,
    entries: [FileEntry],
    virtual_start_index: int,
    virtual_start_column: int,
    viewport_x: float,
    zoom_level: int,
    selection_revision: int,
    show_location: bool,
    empty_message_visible: bool,
    empty_title: string,
    empty_subtitle: string,
    status: string,
    selected_count: int,
    selected_status: string,
    external_edit_active: bool,
    external_edit_status: string,
    undo_available: bool,
    undo_label: string,
    chooser_mode: bool,
    chooser_select_directories: bool,
    chooser_save_mode: bool,
    chooser_accept_label: string,
    focused_selected_path: string,
    chooser_filter_count: int,
    chooser_filter_label: string,
    chooser_choices: [ChooserChoice],
}
